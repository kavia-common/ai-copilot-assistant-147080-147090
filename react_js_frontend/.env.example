# React frontend environment example for AI Copilot
# Copy to .env and adjust as needed for local development.

# Backend API base URL. Defaults to http://localhost:3001 if unset.
# Example for local dev: REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_API_BASE_URL=

# Backend API base URL. Defaults to http://localhost:3001 if unset.
# Example for local dev: REACT_APP_API_BASE_URL=http://localhost:3001
REACT_APP_API_BASE_URL=

# Optional: Supabase configuration (not required to run the app)
# If provided, a Supabase client stub can be conditionally created in the app.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_ANON_KEY=
